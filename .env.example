# Example only (do not commit real secrets).
POSTGRES_PASSWORD=postgres

# --- Docker / platform ---
# Enable if your workspace is on a non-default Windows drive (D:, G:, etc.)
# and Docker Desktop cannot resolve bind-mount paths.
# COMPOSE_CONVERT_WINDOWS_PATHS=1

# Authentication (Phase 10)
# Preferred: configure one or more users via AUTH_USERS as `username:password:role`.
AUTH_USERS=admin:admin:admin,viewer:readonly:read-only
AUTH_SESSION_SECRET=very-secret
#
# Optional: enable password changes + admin resets by persisting users in a file.
# Point this at a writable path mounted into the UI container.
# AUTH_USERS_FILE=/app/data/auth-users.json
#
# Back-compat (if AUTH_USERS is unset, these are used for a single admin user):
AUTH_USERNAME=admin
AUTH_PASSWORD=admin
AUTH_ROLE=admin

# Discovery enrichment (optional).
# IMPORTANT: set DISCOVERY_DEFAULT_SCOPE to your real subnet for Docker bridge
# deployments. Without this, discovery only sees the Docker-internal network.
# Example: DISCOVERY_DEFAULT_SCOPE=192.168.1.0/24
DISCOVERY_DEFAULT_SCOPE=
DISCOVERY_NAME_RESOLUTION_ENABLED=true
DISCOVERY_SNMP_ENABLED=false
DISCOVERY_SNMP_COMMUNITY=public
DISCOVERY_SNMP_VERSION=2c
DISCOVERY_SNMP_TIMEOUT=900ms
DISCOVERY_SNMP_RETRIES=0
DISCOVERY_SNMP_PORT=161
DISCOVERY_ENRICH_MAX_TARGETS=64
DISCOVERY_ENRICH_WORKERS=8

# Phase 7: optional topology enrichment (LLDP/CDP via SNMP).
# NOTE: requires DISCOVERY_SNMP_ENABLED=true and an explicit allowlist.
DISCOVERY_TOPOLOGY_LLDP_ENABLED=false
DISCOVERY_TOPOLOGY_CDP_ENABLED=false
DISCOVERY_TOPOLOGY_ALLOWLIST=10.0.0.0/24

# Phase 7: optional service/port discovery (nmap).
# NOTE: active scanning is disabled by default and requires an explicit allowlist.
DISCOVERY_PORT_SCAN_ENABLED=false
DISCOVERY_PORT_SCAN_ALLOWLIST=10.0.0.0/24
DISCOVERY_PORT_SCAN_PORTS=22,80,443
DISCOVERY_PORT_SCAN_WORKERS=4
DISCOVERY_PORT_SCAN_TIMEOUT=3s
DISCOVERY_PORT_SCAN_MAX_TARGETS=24
