version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "migrations"
    queries:
      - "queries"
    gen:
      go:
        package: "sqlcgen"
        out: "internal/sqlcgen"
        sql_package: "pgx/v5"
