version: "2"
sql:
  - engine: "postgresql"
    schema:
      - "migrations"
    queries:
      - "queries"
    overrides:
      - db_type: "uuid"
        go_type: "string"
      - column: "devices.display_name"
        go_type: "*string"
    gen:
      go:
        package: "sqlcgen"
        out: "internal/sqlcgen"
        sql_package: "pgx/v5"
